export interface CoverSettings {
    title: string;
    subtitle: string;
    author: string;
    icon: string;
    theme: string;
    bgColor: string;
    pattern: string;
    font: string;
    aspectRatio: string;
}
